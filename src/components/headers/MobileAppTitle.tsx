// src/components/headers/MobileAppTitle.tsx
"use client";

import React from "react";

export const MobileAppTitle: React.FC = () => {
  return (
    <div className="px-3 py-2">
      {/* Empty section - removed the title and logo since logo is now in header */}
    </div>
  );
};
